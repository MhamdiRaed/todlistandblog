<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <title><%= post.title %> - Daily Journal</title>
    <style>
        body {
            background: linear-gradient(120deg, #f6d365 0%, #fda085 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .content {
            flex: 1;
            margin: 2rem;
            display: flex;
            flex-direction: column;
            align-items: center; /* Center items horizontally */
        }
        .post-container {
            background-color: #fff; /* White background for the post */
            border-radius: 10px; /* Rounded corners */
            padding: 20px; /* Padding inside the post */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); /* Shadow for the post */
            width: 100%; /* Responsive width */
            max-width: 800px; /* Set a maximum width for posts */
        }
        .post-title {
            color: #dc3545; /* Set a color to match the rest of the site (Bootstrap danger color) */
        }
        .btn-custom {
            background-color: #343a40; /* Dark button color matching the site */
            color: #ffffff; /* White text for button */
        }
        footer {
            background-color: #282c34;
            color: white;
            text-align: center;
            padding: 10px 0;
        }
    </style>
</head>
<body>
    <%- include('header') %>

    <div class="container mt-5 content">
        <div class="post-container">
            <h2 class="post-title text-center"><%= post.title %></h2>
            <p class="text-dark"><%= post.content %></p> <!-- Dark text for post content -->
            <a href="/posts" class="btn btn-custom">Back to Posts</a>
        </div>
    </div>

    <%- include('footer') %>
</body>
</html>
